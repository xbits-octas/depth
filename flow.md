# Depth Camera Processing Flow

## 项目概述

本项目实现双目+结构光+RGB深度相机算法，融合多种深度感知技术，提供高精度的深度图像输出。

## 硬件配置

- **双目灰度相机**: 1280x800 @ 30fps, FOV 60°, 基线距离 10cm
- **结构光投射器**: 近红外斑点, 2万个斑点
- **RGB彩色相机**: 1280x800 @ 30fps, FOV 60°
- **DPU**: 硬件加速视差计算
- **NPU**: 2.5 TOPS@INT8, 深度学习推理

## 主要处理流程

```
┌─────────────────────────────────────────────────────────────────┐
│                        1. 图像采集模块                            │
└─────────────────────────────────────────────────────────────────┘
                                 │
                    ┌────────────┼────────────┐
                    │            │            │
            ┌───────▼──────┐ ┌──▼──────┐ ┌──▼──────┐
            │ 左灰度相机    │ │右灰度相机│ │RGB相机   │
            │ (1280x800)   │ │(1280x800)│ │(1280x800)│
            └───────┬──────┘ └──┬──────┘ └──┬──────┘
                    │            │            │
                    └────────────┼────────────┘
                                 │
┌─────────────────────────────────────────────────────────────────┐
│                        2. 图像预处理                             │
│  - 去噪处理 (Denoise)                                            │
│  - 畸变校正 (Undistortion)                                       │
│  - 立体校正 (Rectification)                                      │
│  - 亮度/对比度调整                                               │
└─────────────────────────────────────────────────────────────────┘
                                 │
                    ┌────────────┴────────────┐
                    │                         │
        ┌───────────▼──────────┐  ┌──────────▼───────────┐
        │  3. 双目立体匹配      │  │ 4. 结构光深度计算     │
        │  (DPU硬件加速)        │  │                      │
        │                       │  │                      │
        │  - 成本计算           │  │  - 斑点检测          │
        │  - 视差优化           │  │  - 斑点匹配          │
        │  - 左右一致性检查     │  │  - 三角测量          │
        │  - 亚像素精化         │  │  - 深度计算          │
        └───────────┬──────────┘  └──────────┬───────────┘
                    │                        │
                    │    ┌──────────────────┐│
                    └────►  双目深度图       ││
                         │  (Disparity Map) ││
                         └──────────┬───────┘│
                                    │        │
                    ┌───────────────┘        │
                    │        ┌───────────────┘
                    │        │  结构光深度图
                    │        │  (Structured Light Depth)
                    │        │
┌───────────────────▼────────▼─────────────────────────────────────┐
│                    5. 深度融合模块                                │
│                                                                   │
│  - 置信度评估 (双目置信度 vs 结构光置信度)                        │
│  - 加权融合 (根据纹理、深度范围、遮挡情况)                        │
│  - 双目优势区: 纹理丰富区域                                       │
│  - 结构光优势区: 弱纹理/重复纹理区域                              │
│  - 输出: 融合深度图                                               │
└─────────────────────────────────────────────────────────────────┘
                                 │
┌─────────────────────────────────────────────────────────────────┐
│                    6. 深度优化模块                                │
│                                                                   │
│  - 孔洞填充 (Hole Filling)                                        │
│  - 边缘保持滤波 (Edge-preserving Filter)                          │
│  - 时间域滤波 (Temporal Filter)                                   │
│  - 空间域滤波 (Spatial Filter)                                    │
│  - 飞点去除 (Flying Pixel Removal)                                │
└─────────────────────────────────────────────────────────────────┘
                                 │
┌─────────────────────────────────────────────────────────────────┐
│              7. AI深度增强模块 (可选, NPU加速)                     │
│                                                                   │
│  - 深度超分辨率                                                   │
│  - 深度边缘细化                                                   │
│  - 深度补全网络                                                   │
│  - 输入: 融合深度图 + RGB图                                       │
│  - 输出: 增强深度图                                               │
│  - 模型: ONNX Runtime (NPU推理)                                   │
└─────────────────────────────────────────────────────────────────┘
                                 │
┌─────────────────────────────────────────────────────────────────┐
│                    8. RGB配准模块                                 │
│                                                                   │
│  - 深度图到RGB图的坐标变换                                        │
│  - 相机外参标定                                                   │
│  - 双线性插值对齐                                                 │
│  - 输出: 配准深度图 + RGB图                                       │
└─────────────────────────────────────────────────────────────────┘
                                 │
┌─────────────────────────────────────────────────────────────────┐
│                    9. 后处理模块                                  │
│                                                                   │
│  - 深度范围限制 (Min/Max Depth)                                   │
│  - 单位转换 (像素视差 → 毫米深度)                                 │
│  - 格式转换                                                       │
└─────────────────────────────────────────────────────────────────┘
                                 │
┌─────────────────────────────────────────────────────────────────┐
│                    10. 输出模块                                   │
│                                                                   │
│  - 深度图 (Depth Map, 16bit/float)                               │
│  - 彩色图 (RGB Image, 8bit)                                       │
│  - 点云 (Point Cloud, XYZ+RGB)                                    │
│  - 置信度图 (Confidence Map)                                      │
└─────────────────────────────────────────────────────────────────┘
```

## 详细处理步骤

### 1. 图像采集模块 (Image Acquisition)

**输入**: 硬件相机接口
**输出**: 左灰度图、右灰度图、RGB图

- 三路相机同步触发采集
- 结构光投射器同步控制
- 时间戳对齐
- 帧缓冲管理

**实现**:
- C++ 硬件驱动接口
- 多线程采集
- 零拷贝传输

---

### 2. 图像预处理 (Image Preprocessing)

**输入**: 原始相机图像
**输出**: 校正后的图像

**步骤**:
1. **去噪处理**: 高斯滤波/中值滤波
2. **畸变校正**: 使用相机内参矩阵和畸变系数
3. **立体校正**: 双目图像行对齐
4. **亮度调整**: 自动曝光补偿

**实现**:
- OpenCV 相机标定库
- 查找表 (LUT) 加速校正
- SIMD 优化

---

### 3. 双目立体匹配 (Stereo Matching - DPU)

**输入**: 左右校正灰度图
**输出**: 视差图 (Disparity Map)

**方法**:
- **算法**: Semi-Global Matching (SGM) 或 Block Matching
- **硬件加速**: DPU 执行密集计算
- **视差范围**: 可配置 (如 0-128 像素)
- **后处理**:
  - 左右一致性检查 (LR Check)
  - 唯一性约束
  - 亚像素精化

**优点**: 纹理丰富区域精度高
**缺点**: 弱纹理/重复纹理区域失效

**实现**:
- DPU 驱动 API
- C++ 接口封装

---

### 4. 结构光深度计算 (Structured Light Depth)

**输入**: 左灰度图 (带投影斑点)
**输出**: 结构光深度图

**步骤**:
1. **斑点检测**:
   - 阈值分割
   - Blob 检测
   - 中心定位

2. **斑点匹配**:
   - 与参考斑点图案匹配
   - 相位解码/灰度编码
   - RANSAC 异常值剔除

3. **三角测量**:
   - 根据投射器-相机几何关系
   - 计算深度值

**优点**: 弱纹理区域有效
**缺点**: 强光干扰、透明/反光表面失效

**实现**:
- CUDA/OpenCL 加速 (可选)
- 优化的斑点匹配算法

---

### 5. 深度融合模块 (Depth Fusion)

**输入**: 双目深度图 + 结构光深度图
**输出**: 融合深度图

**融合策略**:

```cpp
// 伪代码
for each pixel (x, y):
    depth_stereo = stereo_depth_map[x, y]
    depth_sl = structured_light_depth_map[x, y]

    // 置信度评估
    conf_stereo = compute_stereo_confidence(x, y)  // 基于纹理梯度
    conf_sl = compute_sl_confidence(x, y)          // 基于斑点匹配度

    // 加权融合
    if conf_stereo > threshold_high:
        fused_depth = depth_stereo
    else if conf_sl > threshold_high:
        fused_depth = depth_sl
    else:
        // 加权平均
        fused_depth = (conf_stereo * depth_stereo + conf_sl * depth_sl)
                      / (conf_stereo + conf_sl)
```

**置信度计算**:
- **双目置信度**: 纹理梯度、匹配成本、一致性检查
- **结构光置信度**: 斑点数量、匹配误差、投影对比度

**实现**:
- C++ 高性能实现
- OpenMP 并行化

---

### 6. 深度优化模块 (Depth Refinement)

**输入**: 融合深度图
**输出**: 优化深度图

**方法**:
1. **孔洞填充**:
   - 基于背景扩散
   - 双边插值

2. **边缘保持滤波**:
   - 双边滤波 (Bilateral Filter)
   - 导向滤波 (Guided Filter)
   - Joint-Bilateral Filter (以RGB图为引导)

3. **时间域滤波**:
   - IIR 滤波器: `depth_t = alpha * depth_t + (1-alpha) * depth_t-1`
   - 运动检测 (避免拖影)

4. **飞点去除**:
   - 统计滤波
   - 边缘深度不连续检测

**实现**:
- OpenCV 图像处理
- 自定义优化算法

---

### 7. AI深度增强模块 (AI Depth Enhancement)

**输入**: 融合深度图 + RGB图
**输出**: 增强深度图

**网络架构** (可选方案):
- **单图深度估计**: MobileNet-based depth prediction
- **深度补全**: Sparse-to-Dense 网络
- **深度超分辨率**: EDSR/DKN

**训练**:
- **框架**: PyTorch
- **数据集**: 自采集数据 + 公开数据集 (NYU Depth V2, KITTI)
- **损失函数**: L1 Loss + SSIM Loss + Edge Loss

**部署**:
- **导出**: PyTorch → ONNX
- **推理**: ONNX Runtime (CPU) / NPU工具链 (嵌入式)
- **优化**: INT8量化, 模型剪枝

**实现**:
- Python 训练脚本 (`python/training/`)
- C++ ONNX Runtime 推理 (`src/core/ai_depth.cpp`)

---

### 8. RGB配准模块 (RGB Registration)

**输入**: 深度图 (参考左灰度相机) + RGB图
**输出**: 配准深度图 (对齐到RGB相机视角)

**步骤**:
1. **相机标定**: 获取RGB相机相对于左灰度相机的外参 (R, T)
2. **坐标变换**:
   ```
   P_left = depth_to_3D(depth, K_left)
   P_rgb = R * P_left + T
   (u, v) = project_to_2D(P_rgb, K_rgb)
   ```
3. **插值**: 双线性插值生成对齐深度图

**实现**:
- C++ 矩阵运算
- Eigen 库

---

### 9. 后处理模块 (Post-processing)

**输入**: 配准深度图
**输出**: 最终深度图

**操作**:
- **深度范围限制**: 裁剪到 [min_depth, max_depth]
- **单位转换**: 视差 → 深度 (毫米)
  ```
  depth_mm = (baseline * focal_length) / disparity
  ```
- **格式转换**: float32 → uint16 (如需要)

---

### 10. 输出模块 (Output)

**输出格式**:
1. **深度图**:
   - 16bit PNG / 32bit float TIFF
   - 单位: 毫米

2. **RGB图**:
   - 8bit PNG / JPEG

3. **点云**:
   - PLY / PCD 格式
   - XYZ + RGB

4. **置信度图**:
   - 8bit 灰度图 (0-255)

**实现**:
- OpenCV / PCL 库
- 自定义文件IO

---

## 性能指标

| 指标 | 目标值 |
|------|--------|
| 处理帧率 | ≥30 fps |
| 深度精度 | ≤1% @ 1m |
| 深度范围 | 0.3m - 10m |
| 延迟 | ≤50ms |
| 功耗 | ≤5W |

---

## 技术栈

### 开发阶段
- **语言**: Python, C++14/17
- **深度学习**: PyTorch
- **计算机视觉**: OpenCV, PCL
- **数值计算**: NumPy, Eigen

### 部署阶段
- **推理引擎**: ONNX Runtime
- **嵌入式平台**: NPU工具链 (厂商SDK)
- **构建系统**: CMake
- **加速**: OpenMP, SIMD

---

## 版本管理

- **格式**: vx.yy.zzz
  - `x`: 大版本 (架构变更)
  - `yy`: 次版本 (功能增加)
  - `zzz`: 小版本 (Bug修复, 自动递增)

- **Git工作流**:
  - 每次操作前提交
  - 保持可回退状态
  - 清晰的commit message

---

## 未来优化方向

1. **多帧融合**: 利用时序信息提升精度
2. **SLAM集成**: 相机运动估计, 动态场景处理
3. **语义分割**: 结合物体类别优化深度
4. **ToF融合**: 增加ToF传感器 (如有)
5. **端到端学习**: 整体pipeline神经网络化

---

## 参考文献

1. Hirschmuller, H. (2007). Stereo Processing by Semiglobal Matching and Mutual Information. TPAMI.
2. Zhang, S. (2018). High-speed 3D shape measurement with structured light methods. Opt. Lasers Eng.
3. Godard, C. et al. (2019). Digging Into Self-Supervised Monocular Depth Estimation. ICCV.

---

*文档版本: v0.01.001*
*最后更新: 2025-11-12*
